# 🚂 Деплой на Railway — АКТУАЛЬНАЯ ВЕРСИЯ

> Связано: [ТЗ](ТЗ_выгрузка_чатов_RetailCRM.md) | [Архитектура](Архитектура.md) | [Бэклог](Бэклог.md) | 

---


> **Последнее обновление:** 2026-01-22
> **Статус:** Задеплоено и работает

---

## Текущая конфигурация

### GitHub репозиторий
- **URL:** https://github.com/bootchq/instinto-analytics
- **Аккаунт:** bootchq

### Railway проект
- **Проект:** instinto-analytics
- **Аккаунт:** bootchq (GitHub)

### Сервисы в Railway

| Сервис | Назначение | Расписание (cron) |
|--------|------------|-------------------|
| `instinto-analytics` | Полная выгрузка чатов | Ручной запуск |
| `daily-report` | Ежедневный отчёт в Telegram | `0 6 * * *` (09:00 MSK) |
| `weekly-report` | Еженедельный отчёт | `59 20 * * 0` (вс 23:59 MSK) |

### Ключевые особенности
- **Playwright** — автоматический логин в RetailCRM (без ручного обновления токенов)
- **Dockerfile** — образ с Playwright для headless браузера
- **Telegram** — автоматическая отправка отчётов

---

## Быстрые команды

```bash
# Проверить статус
cd /tmp/instinto-analytics && railway service status --all

# Логи сервиса
railway logs --service instinto-analytics

# Перезапуск
railway service redeploy --service instinto-analytics --yes

# Добавить переменную
railway variables set --service instinto-analytics KEY=value
```

---

## Переменные окружения (актуальные)

---

## 🚀 Шаг 1: Создание проекта на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий (или создайте новый и загрузите код)

---

## 🔐 Шаг 2: Настройка переменных окружения

В Railway перейдите в **Settings → Variables** и добавьте все переменные:

### RetailCRM
```
RETAILCRM_URL=https://instinto.retailcrm.ru
RETAILCRM_API_KEY=YF6FMd9pxeNtdBajaLFLs1FJgiQSuHqo
```

### Google Sheets
```
GOOGLE_SHEETS_ID=1to83Pw9vjl6p1RnnrJT-qtHc85x5s2U_qYp6jSZKhYM
```

**⚠️ ВАЖНО:** `GOOGLE_SERVICE_ACCOUNT_JSON` нужно добавить как **секрет** (Secret):
1. Скопируйте **весь JSON** из файла `retailcrm-482419-9480820e32ab.json`
2. В Railway: **Settings → Variables → New Variable**
3. Название: `GOOGLE_SERVICE_ACCOUNT_JSON`
4. Значение: вставьте весь JSON (одной строкой или многострочно)
5. Отметьте как **Secret**

### Период анализа
```
START_DATE=2025-10-01
END_DATE=2025-12-26
```

### Настройки
```
CHANNELS=whatsapp,instagram
TZ=Europe/Moscow
WORK_HOURS=10:00-23:00
```

### Ограничители
```
MAX_MESSAGES_PER_CHAT=500
MAX_TOTAL_MESSAGES=200000
WEB_TIMEOUT_S=180
WEB_RETRIES=5
```

### Telegram (опционально)
```
TELEGRAM_ENABLED=1
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHAT_ID=ваш_chat_id
```

### Скрипт для запуска
```
RAILWAY_SCRIPT=run_all
```

---

## 📁 Шаг 3: Добавление curl-файлов

Railway не поддерживает загрузку файлов напрямую, поэтому нужно добавить их содержимое как переменные окружения:

### Вариант А: Через переменные окружения (рекомендую)

1. Скопируйте содержимое `web_curl.txt`
2. В Railway: **Settings → Variables → New Variable**
3. Название: `WEB_CURL_CONTENT`
4. Значение: вставьте весь текст из `web_curl.txt`
5. Отметьте как **Secret** (там могут быть токены)

Аналогично для `web_messages_curl.txt`:
- Название: `WEB_MESSAGES_CURL_CONTENT`
- Значение: содержимое файла
- Отметьте как **Secret**

### Вариант Б: Через файлы в репозитории

Если файлы не содержат секретов, можно добавить их в репозиторий (но **НЕ рекомендуется**, если там есть токены/cookies).

---

## 🔧 Шаг 4: Модификация скриптов для Railway

Нужно обновить `export_to_sheets.py`, чтобы он мог читать curl-файлы из переменных окружения:

```python
# В начале функции main() добавить:

# Если есть переменные окружения с curl-контентом, создаём файлы
if os.environ.get("WEB_CURL_CONTENT"):
    web_curl_path = Path(__file__).parent / "web_curl.txt"
    web_curl_path.write_text(os.environ["WEB_CURL_CONTENT"], encoding="utf-8")

if os.environ.get("WEB_MESSAGES_CURL_CONTENT"):
    web_messages_curl_path = Path(__file__).parent / "web_messages_curl.txt"
    web_messages_curl_path.write_text(os.environ["WEB_MESSAGES_CURL_CONTENT"], encoding="utf-8")
```

Также нужно обновить путь к Google Service Account JSON:

```python
# Вместо пути к файлу используем переменную окружения
google_json_content = os.environ.get("GOOGLE_SERVICE_ACCOUNT_JSON")
if google_json_content:
    # Если это JSON-строка, сохраняем во временный файл
    import json
    import tempfile
    
    # Проверяем, это путь или JSON-строка
    if google_json_content.strip().startswith("{"):
        # Это JSON-строка
        temp_file = tempfile.NamedTemporaryFile(mode='w', suffix='.json', delete=False)
        json.dump(json.loads(google_json_content), temp_file)
        temp_file.close()
        google_json_path = temp_file.name
    else:
        # Это путь к файлу
        google_json_path = google_json_content
else:
    raise RuntimeError("GOOGLE_SERVICE_ACCOUNT_JSON не задан")
```

---

## ⏰ Шаг 5: Настройка расписания

### Вариант А: Railway Scheduler (платный)

1. В Railway: **Settings → Schedules**
2. Создайте новое расписание:
   - **Cron expression**: `0 9 * * *` (каждый день в 9:00 UTC)
   - **Command**: `python run_railway.py`
   - **Environment**: выберите ваш сервис

### Вариант Б: Cron через Railway (бесплатно)

Создайте файл `.railway/cron`:

```
0 9 * * * python run_railway.py
```

### Вариант В: Внешний cron (например, cron-job.org)

1. Зарегистрируйтесь на [cron-job.org](https://cron-job.org)
2. Создайте задачу:
   - **URL**: `https://ваш-railway-сервис.up.railway.app/run` (если настроен webhook)
   - **Расписание**: каждый день в 9:00 UTC

---

## 🧪 Шаг 6: Тестирование

1. В Railway: **Deployments → Manual Deploy**
2. Или через CLI:
   ```bash
   railway up
   ```

3. Проверьте логи:
   ```bash
   railway logs
   ```

---

## 📊 Мониторинг

- **Логи**: Railway → Deployments → View Logs
- **Метрики**: Railway → Metrics
- **Уведомления**: Railway → Settings → Notifications

---

## 🔄 Обновление кода

После изменений в коде:
1. Закоммитьте изменения в Git
2. Railway автоматически задеплоит новую версию
3. Или вручную: **Deployments → Redeploy**

---

## ⚠️ Важные замечания

1. **Таймауты**: Railway имеет лимит на время выполнения (обычно 5-10 минут для бесплатного плана). Для долгих задач может понадобиться платный план.

2. **Хранилище**: Railway не сохраняет файлы между запусками. Если нужны временные файлы, используйте `/tmp`.

3. **Секреты**: Все чувствительные данные (токены, JSON) должны быть в **Secrets**, а не в обычных переменных.

4. **Лимиты API**: Убедитесь, что не превышаете лимиты RetailCRM API и Google Sheets API.

---

## 🆘 Решение проблем

### Ошибка: "File not found: web_curl.txt"
→ Добавьте переменные `WEB_CURL_CONTENT` и `WEB_MESSAGES_CURL_CONTENT`

### Ошибка: "GOOGLE_SERVICE_ACCOUNT_JSON not found"
→ Добавьте JSON как переменную окружения (Secret)

### Ошибка: "Timeout"
→ Увеличьте таймауты или используйте платный план Railway

### Ошибка: "Connection reset"
→ Это нормально, скрипт использует fallback на lastMessage

---

## 📝 Чеклист перед деплоем

- [ ] Все переменные окружения добавлены в Railway
- [ ] `GOOGLE_SERVICE_ACCOUNT_JSON` добавлен как Secret
- [ ] `WEB_CURL_CONTENT` и `WEB_MESSAGES_CURL_CONTENT` добавлены как Secrets
- [ ] Код обновлён для чтения из переменных окружения
- [ ] Расписание настроено
- [ ] Тестовый запуск выполнен успешно

---

**Готово! Теперь скрипты будут работать на Railway автоматически.** 🎉

