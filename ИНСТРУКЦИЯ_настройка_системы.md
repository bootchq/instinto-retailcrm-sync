# Инструкция по настройке системы анализа работы менеджеров

> Связано: [ТЗ](ТЗ_выгрузка_чатов_RetailCRM.md) | [Архитектура](Архитектура.md) | [Бэклог](Бэклог.md) | 

---


## 📋 Что делает система

1. **Ежедневный анализ** — автоматически каждый день в 9:00
2. **SPIN-анализ** — анализ по методике SPIN-продаж
3. **Анализ типов вопросов** — открытые vs закрытые
4. **Связь с конверсией** — анализ влияния SPIN на заказы
5. **Ежедневные отчёты в Telegram** — автоматическая отправка
6. **Уведомления об ухудшении** — предупреждения при снижении метрик
7. **Шаблоны диалогов** — готовые скрипты на основе лучших практик

---

## 🚀 Быстрый старт

### 1. Настройка Telegram-бота

1. Создайте бота через [@BotFather](https://t.me/BotFather):
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Сохраните `TELEGRAM_BOT_TOKEN`

2. Узнайте `TELEGRAM_CHAT_ID`:
   - Напишите боту в личку "test"
   - Запустите: `python telegram_get_chat_id.py`
   - Или в браузере: `https://api.telegram.org/botTOKEN/getUpdates`

3. Добавьте в файл `env`:
   ```
   TELEGRAM_BOT_TOKEN="ваш_токен"
   TELEGRAM_CHAT_ID="ваш_chat_id"
   ```

### 2. Установка зависимостей

```bash
cd "/Users/noor/Documents/Obsidian Vault/Cursor/INSTINTO/Анализ работы менеджеров/retailcrm_chats"
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 3. Настройка автоматического запуска

```bash
# Копируем plist файл
mkdir -p ~/Library/LaunchAgents
cp launchd/ru.instinto.retailcrm.daily.plist ~/Library/LaunchAgents/

# Загружаем задачу
launchctl unload ~/Library/LaunchAgents/ru.instinto.retailcrm.daily.plist 2>/dev/null || true
launchctl load ~/Library/LaunchAgents/ru.instinto.retailcrm.daily.plist

# Проверяем статус
launchctl list | grep ru.instinto.retailcrm.daily
```

**Важно:** Автоматический запуск работает только когда Mac включён и есть интернет. Если Mac спит — задача не выполнится.

---

## 📊 Запуск анализов вручную

### Ежедневный отчёт (с отправкой в Telegram)
```bash
cd "/Users/noor/Documents/Obsidian Vault/Cursor/INSTINTO/Анализ работы менеджеров/retailcrm_chats"
source .venv/bin/activate
python telegram_daily_report.py
```

### SPIN-анализ
```bash
python spin_analysis.py
```

### Анализ типов вопросов
```bash
python question_type_analysis.py
```

### Анализ связи SPIN с конверсией
```bash
python spin_conversion_analysis.py
```

### Генерация шаблонов диалогов
```bash
python dialog_templates.py
```

### Анализ лучших практик
```bash
python best_practices_analysis.py
```

### Выделение топ-5 проблем
```bash
python weekly_metrics_analysis.py
```

---

## 📈 Что в Google Sheets

После запуска анализов в таблице появятся листы:

- `spin_manager_metrics` — метрики SPIN по менеджерам
- `spin_chat_metrics` — метрики SPIN по каждому чату
- `spin_improvement_plans` — планы улучшения
- `manager_top_problems` — топ-5 проблем каждого менеджера
- `weekly_metrics_tracking` — еженедельные метрики для отслеживания
- `improvement_steps_detailed` — детальные шаги улучшения
- `daily_report` — ежедневные отчёты с изменениями
- `question_type_analysis` — анализ открытых/закрытых вопросов
- `spin_conversion_correlation` — связь SPIN с конверсией
- `best_chats_analysis` — анализ лучших чатов
- `best_scripts` — готовые скрипты
- `dialog_templates_full` — полные шаблоны диалогов

---

## 🔔 Telegram-уведомления

Ежедневно в 9:00 (если Mac включён) вы получите:

1. **Основной отчёт** с метриками каждого менеджера:
   - SPIN Полнота цикла
   - Этапы S, P, I, N
   - Количество вопросов
   - Скорость ответа
   - Response Rate

2. **Предупреждения** (если метрики ухудшились):
   - ⚠️ Снижение SPIN Полноты на X%
   - 🔴 Критически низкие значения
   - ⚠️ Снижение количества вопросов

---

## 🎯 Индивидуальные цели для менеджеров

Система учитывает, что у менеджеров:
- **Одинаковые KPI** (конверсия, средний чек, выручка)
- **Разные стартовые условия** (один лучше в S, другой в N)
- **Разные методы работы**

Поэтому цели устанавливаются индивидуально на основе текущих показателей.

---

## 📝 KPI и вознаграждения

### KPI 1: Выполнение плана (%)
- 1.5% от выручки при выполнении до 85%
- 2.5% от выручки при выполнении 85-95%
- 4.5% от выручки при выполнении 95-100%
- 6% от выручки при выполнении от 100%

### KPI 2: Средний чек (₽)
- 500₽ за неделю до 4600₽
- 1500₽ за неделю 4600-5600₽
- 2000₽ за неделю 5600-6600₽
- 3000₽ за неделю от 6600₽

### KPI 3: Конверсия (%)
- 500₽ за неделю до 6.4%
- 1000₽ за неделю 6.4-7.4%
- 1500₽ за неделю 7.4-8.4%
- 3000₽ за неделю от 8.4%

**Приоритет системы:** Улучшение показателей, которые влияют на конверсию и средний чек.

---

## 🛠 Устранение неполадок

### Telegram-бот не отправляет сообщения
1. Проверьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` в файле `env`
2. Проверьте, что бот запущен: напишите ему в личку
3. Проверьте логи: `cat daily_run.err.log`

### Автоматический запуск не работает
1. Проверьте статус: `launchctl list | grep ru.instinto.retailcrm.daily`
2. Проверьте логи: `cat daily_run.out.log` и `cat daily_run.err.log`
3. Перезагрузите задачу:
   ```bash
   launchctl unload ~/Library/LaunchAgents/ru.instinto.retailcrm.daily.plist
   launchctl load ~/Library/LaunchAgents/ru.instinto.retailcrm.daily.plist
   ```

### Нет данных в Google Sheets
1. Проверьте `GOOGLE_SHEETS_ID` и `GOOGLE_SERVICE_ACCOUNT_JSON` в файле `env`
2. Убедитесь, что service account имеет доступ к таблице
3. Запустите `export_to_sheets.py` для первичной выгрузки данных

---

## 📚 Дополнительные материалы

- `План_улучшения_еженедельный.md` — план улучшения с еженедельным контролем
- `План_улучшения_SPIN.md` — детальный план по SPIN-методике
- `Дополнительные_вопросы.md` — ответы на вопросы по системе

---

## ✅ Чек-лист настройки

- [ ] Telegram-бот создан и настроен
- [ ] `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` добавлены в `env`
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Первичная выгрузка данных выполнена (`python export_to_sheets.py`)
- [ ] Автоматический запуск настроен (`launchctl load`)
- [ ] Тестовый запуск выполнен (`python telegram_daily_report.py`)
- [ ] Отчёт пришёл в Telegram

---

**Готово! Система настроена и готова к работе.** 🚀




