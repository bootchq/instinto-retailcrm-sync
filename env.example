# Скопируйте в файл `env` рядом и заполните значения.

# RetailCRM
RETAILCRM_URL="https://yourdomain.retailcrm.ru"
RETAILCRM_API_KEY="paste_api_key_here"

# Google Sheets
GOOGLE_SHEETS_ID="spreadsheet_id_from_url"
GOOGLE_SERVICE_ACCOUNT_JSON="/abs/path/to/service_account.json"

# Period (YYYY-MM-DD)
# Вариант 1: Фиксированные даты
START_DATE="2025-10-01"
END_DATE="2025-12-26"

# Вариант 2: Автоматически за последние N дней (приоритетнее, чем START_DATE/END_DATE)
# LAST_DAYS=60

# Filters / settings
CHANNELS="whatsapp,instagram"
TZ="Europe/Moscow"
WORK_HOURS="10:00-23:00"

# Опционально: файлы для web GraphQL
WEB_CURL_FILE="web_curl.txt"
WEB_MESSAGES_CURL_FILE="web_messages_curl.txt"

# Ограничители, чтобы не упереться в лимиты Google Sheets
MAX_MESSAGES_PER_CHAT="500"
MAX_TOTAL_MESSAGES="200000"

# Стабильность web GraphQL (mg-s1.retailcrm.pro) при долгих выгрузках
WEB_TIMEOUT_S="180"
WEB_RETRIES="5"

# Telegram weekly report (optional)
# Как получить:
# 1) В Telegram: @BotFather → /newbot → получишь TELEGRAM_BOT_TOKEN
# 2) Узнать chat_id:
#    - личка: напиши боту, потом открой `https://api.telegram.org/bot<TOKEN>/getUpdates` и возьми message.chat.id
#    - группа: добавь бота в группу, напиши сообщение, аналогично getUpdates, chat.id будет отрицательным
TELEGRAM_ENABLED="0"
TELEGRAM_BOT_TOKEN=""
TELEGRAM_CHAT_ID=""


